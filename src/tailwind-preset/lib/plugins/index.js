const boxShadow = require('./box-shadow');
const iconSize = require('./icon-size');
const inlineHeight = require('./inline-height');
const lang = require('./lang');
const leadingText = require('./leading-text');
const size = require('./size');
const theme = require('./theme');

module.exports = [
  size,
  boxShadow,
  iconSize,
  leadingText,
  inlineHeight,
  lang,
  theme,
];
